# Git 增强插件 - 使用说明

> 简洁版使用指南 | 完整文档请参考 [README_CN.md](README_CN.md)

## 📖 插件功能概览

这是一个为 IntelliJ IDEA 设计的 Git 工作流增强插件，提供三个核心功能：

### 功能 1：智能变基推送
**一句话描述**：在一个对话框中完成"提交代码 → 变基到目标分支 → 强制推送 → 自动创建 MR/PR"的全流程操作。

**核心价值**：
- 替代繁琐的命令行操作（通常需要 5-8 条 Git 命令）
- 支持灵活开关变基功能（可以只提交推送，不变基）
- 自动处理未提交文件
- 可选自动创建 GitLab MR 或 GitHub PR

### 功能 2：AI 提交消息生成
**一句话描述**：基于代码变更自动生成规范的中文提交消息。

**核心价值**：
- 分析 `git diff` 和文件变更，智能生成提交消息
- 支持 OpenAI、Azure OpenAI、本地模型（Ollama）等任意兼容端点
- 遵循提交消息最佳实践（动词开头、简洁明了）

### 功能 3：Arthas 热修复脚本生成
**一句话描述**：从 `.java`/`.kt` 源文件或 `.class` 文件一键生成 Base64 编码的 Arthas 热修复 Shell 脚本。

**核心价值**：
- 生产环境紧急修复 Bug，无需重启服务
- 自动查找源文件对应的编译后 `.class` 文件
- 生成的脚本包含 SHA-256 校验，确保文件完整性
- 支持批量处理多个类文件

---

## 🚀 功能一：智能变基推送

### 这个功能解决什么问题？

在日常开发中，我们经常需要：
1. 将 feature 分支同步到最新的 master/develop 分支（避免合并冲突）
2. 提交代码并推送到远程
3. 创建 Merge Request 或 Pull Request

传统做法需要执行多条命令：
```bash
git add .
git commit -m "..."
git fetch origin master
git rebase origin/master
# 如果有冲突，需要手动解决
git push --force-with-lease
# 然后打开浏览器手动创建 MR/PR
```

**这个功能将以上所有步骤整合到一个对话框中完成。**

### 核心特性详解

#### 1. 灵活的变基开关
- **开启"变基到目标分支"**（默认）：执行 `fetch → rebase → push --force-with-lease`
- **关闭"变基到目标分支"**：跳过 fetch 和 rebase，直接执行普通 `push`
  - 适用场景：只想推送代码，不想变基
  - 适用场景：目标分支已经是最新的，不需要重复变基

#### 2. 智能分支建议
对话框会自动推荐目标分支，优先级如下：
1. 用户在设置中配置的默认分支
2. `master` 分支（如果存在且不是当前分支）
3. `main` 分支（如果存在且不是当前分支）
4. `develop` 分支（如果存在且不是当前分支）
5. 其他远程分支

**支持分支模糊搜索**：下拉框可编辑，输入关键词自动过滤分支列表。

#### 3. 自动处理未提交文件
- 如果工作区有未提交的文件，插件会自动执行 `git add .` 和 `git commit`
- 提交消息可以手动输入，也可以通过"AI生成"按钮自动生成

#### 4. 安全的强制推送
- 使用 `--force-with-lease` 而非 `--force`
- 原理：只有在远程分支没有新提交时才允许强制推送
- 效果：防止覆盖团队成员的提交

#### 5. 自动创建 MR/PR
- 勾选"推送后自动创建 merge 请求"
- 插件会自动识别 Git 平台（GitLab 或 GitHub）
- 调用 REST API 创建 Merge Request 或 Pull Request
- 创建成功后显示链接，可直接点击打开

### 使用步骤

**方式 1：从提交窗口启动（推荐）**

1. 修改代码后，按 `Ctrl+K`（Windows/Linux）或 `Cmd+K`（Mac）
2. 在提交窗口中，点击 **"变基并推送"** 按钮（而不是普通的"Commit"按钮）
3. 在弹出的对话框中配置：
   - **目标分支**：选择要变基到的分支（如 `master`）
   - **提交消息**：输入或点击"AI生成"
   - **变基到目标分支**：决定是否执行变基（默认勾选）
   - **推送后自动创建 merge 请求**：是否自动创建 MR/PR（可选）
4. 点击 **"变基并推送"**
5. 等待操作完成（对话框会显示实时进度）

**方式 2：从菜单启动**
- 菜单栏：`Git` → `Rebase and Push`

### 执行流程图解

```
开始
 ↓
检查是否有未提交的文件？
 ↓ 是
执行 git add . && git commit -m "..."
 ↓
检查"变基到目标分支"是否勾选？
 ↓ 是
执行 git fetch origin <target-branch>
 ↓
执行 git rebase origin/<target-branch>
 ↓ 有冲突？
   ↓ 是 → 显示错误，提示手动解决
   ↓ 否
执行 git push --force-with-lease
 ↓
检查"自动创建 MR"是否勾选？
 ↓ 是
调用 GitLab/GitHub API 创建 MR/PR
 ↓
显示成功通知（包含 MR/PR 链接）
 ↓
结束
```

### 注意事项
- ✅ 变基前会先提交所有未提交的文件，确保工作区干净
- ✅ 使用 `--force-with-lease` 替代 `--force`，更加安全
- ⚠️ 如果变基时出现冲突，需要手动解决后再推送（见常见问题 Q1）
- ⚠️ 多仓库项目只操作第一个 Git 仓库
- 💡 首次创建 MR/PR 需要配置 Personal Access Token（见配置章节）

---

## 🤖 功能二：AI 提交消息生成

### 这个功能解决什么问题？

编写规范的提交消息是一项重复且耗时的工作：
- 需要总结代码变更
- 需要遵循团队的提交规范
- 需要使用适当的动词和格式

**这个功能自动分析代码变更，生成规范的中文提交消息。**

### 工作原理

```
用户点击"AI生成"
 ↓
插件收集信息：
 - git diff（代码变更详情）
 - 变更文件列表
 - 当前分支名称
 ↓
构造提示词（Prompt）
 ↓
调用 OpenAI 兼容 API
 ↓
解析 AI 响应
 ↓
填充到提交消息输入框
```

**生成的消息特点**：
- 使用中文
- 动词开头（如"修复"、"添加"、"优化"）
- 简洁明了（通常 1-2 行）
- 可手动编辑修改

### 前置配置

**步骤 1：打开设置页面**
1. 打开设置：`File` → `Settings`（Windows/Linux）或 `Preferences`（Mac）
2. 导航到：`Tools` → `Git Rebase & Push`

**步骤 2：配置 OpenAI API**
填写三个必填字段：

| 字段 | 说明 | 示例 |
|------|------|------|
| **Base URL** | API 端点地址（需以 `/v1` 结尾） | `https://api.openai.com/v1` |
| **Model ID** | 模型名称 | `gpt-4`、`gpt-3.5-turbo` |
| **API Key** | API 密钥 | `sk-proj-xxxxxxxxxx` |

**步骤 3：测试连接**
- 点击 **"Test Connection"** 按钮
- 如果配置正确，会显示"连接成功"
- 如果失败，会显示具体错误信息

**步骤 4：保存设置**
- 点击 **"Apply"** 或 **"OK"** 保存

### 使用步骤

1. 在"变基并推送"对话框中，找到"提交消息"输入框
2. 点击输入框下方的 **"AI生成"** 按钮
3. 等待 2-5 秒（取决于 API 响应速度）
4. AI 生成的提交消息会自动填充到输入框
5. 根据需要手动修改消息
6. 继续完成变基推送流程

### 支持的 API 提供商

**1. OpenAI 官方 API**
```
Base URL: https://api.openai.com/v1
Model ID: gpt-4 或 gpt-3.5-turbo
API Key: sk-proj-xxxxxxxxxxxxxxxx
```
- 优点：质量最高，响应快
- 缺点：需要国际网络，有使用成本

**2. Azure OpenAI**
```
Base URL: https://your-resource.openai.azure.com/openai/deployments/your-deployment
Model ID: gpt-4
API Key: your-azure-key
```
- 优点：企业级部署，符合合规要求
- 缺点：需要 Azure 订阅

**3. 国内 API 服务（以智谱 AI 为例）**
```
Base URL: https://open.bigmodel.cn/api/paas/v4
Model ID: glm-4
API Key: your-api-key
```
- 优点：国内访问速度快，支持中文
- 缺点：需要注册账号

**4. 本地模型（Ollama）**
```
Base URL: http://localhost:11434/v1
Model ID: qwen:7b 或 deepseek-coder
API Key: (留空或任意值)
```
- 优点：完全离线，无隐私泄露，零成本
- 缺点：需要本地部署，质量取决于模型

**5. 其他兼容 OpenAI API 的服务**
只要服务实现了 OpenAI 的 `/v1/chat/completions` 接口，都可以使用。

### 注意事项
- ⚠️ AI 生成的消息可能不完全准确，建议人工审核
- 💡 对于大型代码变更，AI 可能需要更长时间分析
- 💡 如果网络不佳，生成可能失败，可多次尝试
- 🔒 代码 diff 会发送到 API 端点，注意隐私和安全

---

## 🔥 功能三：Arthas 热修复脚本生成

### 这个功能解决什么问题？

生产环境出现 Bug 时，传统的修复流程：
1. 修复代码
2. 编译打包
3. 上传到服务器
4. 重启服务（可能导致业务中断）

使用 **阿里 Arthas** 可以实现 Java 类的热替换（无需重启），但需要：
- 将编译后的 `.class` 文件上传到服务器
- 确保文件完整性
- 手动执行 Arthas 命令

**这个功能一键生成包含 Base64 编码 class 文件的自包含 Shell 脚本，简化整个流程。**

### 核心特性详解

#### 1. 智能查找编译文件
- **支持源文件**：在 `.java` 或 `.kt` 文件上右键，自动查找对应的 `.class`
- **支持编译文件**：直接在 `.class` 文件上右键使用
- **多模块支持**：自动识别模块的输出目录
- **搜索路径**：
  - IntelliJ 编译输出目录（`compilerOutputPath`）
  - Maven 标准目录：`target/classes/`
  - Gradle Kotlin：`build/classes/kotlin/main/`
  - Gradle Java：`build/classes/java/main/`
  - IntelliJ 默认：`out/production/<module>/`

#### 2. 生成脚本内容
生成的 Shell 脚本包含：
```bash
#!/bin/bash
# Arthas Hotfix Script for UserService
# Generated: 2026-07-27T12:34:56Z
# Source SHA256: a1b2c3d4...

# 1. Base64 编码的 .class 文件（gzip 压缩）
CLASS_DATA="H4sIAAAAAAAA/..."

# 2. 解码和解压缩
echo "$CLASS_DATA" | base64 -d | gunzip > /tmp/UserService_20260727_123456.class

# 3. SHA-256 校验（确保文件完整性）
echo "a1b2c3d4... /tmp/UserService_20260727_123456.class" | sha256sum -c
# 如果 sha256sum 不可用，尝试 shasum -a 256（macOS）

# 4. 打印 Arthas retransform 命令
echo "File ready. In Arthas, execute:"
echo "retransform /tmp/UserService_20260727_123456.class"
```

#### 3. 安全特性
- **SHA-256 校验**：确保传输过程中文件未损坏
- **gzip 压缩**：减小脚本体积（通常压缩 50-70%）
- **Base64 编码**：确保脚本可以安全传输（邮件、聊天工具）
- **时间戳命名**：避免文件名冲突

#### 4. 批量支持
- 可同时选中多个 `.class` 文件
- 会生成包含所有类的脚本
- 每个类独立编码和校验

### 使用步骤

#### 步骤 1：修复代码并编译

```bash
# Maven 项目
mvn compile

# Gradle 项目
./gradlew compileKotlin
# 或
./gradlew compileJava

# IntelliJ IDEA
# 菜单：Build → Build Project（Ctrl+F9）
```

#### 步骤 2：生成脚本

**方式 1：从源文件生成（推荐）**
1. 在项目视图中找到修改的 `.java` 或 `.kt` 源文件
2. 右键点击源文件
3. 选择 **"Generate Arthas Hotfix Script"**
4. 插件会自动查找对应的编译后 `.class` 文件
5. 如果找到多个（如内部类），会弹出选择列表

**方式 2：从编译文件生成**
1. 在项目视图中导航到编译输出目录：
   - Maven：`target/classes/com/example/YourClass.class`
   - Gradle：`build/classes/kotlin/main/com/example/YourClass.class`
2. 右键点击 `.class` 文件
3. 选择 **"Generate Arthas Hotfix Script"**

**方式 3：从编辑器标签页生成**
1. 在编辑器中打开源文件
2. 右键点击编辑器标签页
3. 选择 **"Generate Arthas Hotfix Script"**

**方式 4：从 Tools 菜单生成**
- 菜单栏：`Tools` → `Generate Arthas Hotfix Script`
- 会提示选择文件

#### 步骤 3：保存或复制脚本

在弹出的对话框中：
- **Copy to Clipboard**：复制脚本内容到剪贴板（适合快速粘贴）
- **Save to File**：保存为 `.sh` 文件（建议命名如 `hotfix_20260727.sh`）
- **Close**：仅查看，不保存

#### 步骤 4：上传到生产服务器

```bash
# 使用 SCP 上传
scp hotfix_20260727.sh user@production-server:/tmp/

# 或使用 SFTP、FTP 等工具
```

#### 步骤 5：在服务器上执行脚本

```bash
# 1. SSH 登录服务器
ssh user@production-server

# 2. 赋予执行权限
chmod +x /tmp/hotfix_20260727.sh

# 3. 执行脚本
/tmp/hotfix_20260727.sh

# 输出示例：
# /tmp/UserService_20260727_123456.class: OK
# File ready. In Arthas, execute:
# retransform /tmp/UserService_20260727_123456.class
```

#### 步骤 6：使用 Arthas 热替换

```bash
# 1. 启动 Arthas（如果未运行）
java -jar arthas-boot.jar

# 2. 选择目标 Java 进程
# Arthas 会列出所有 Java 进程，输入对应编号

# 3. 执行热替换命令（从脚本输出复制）
retransform /tmp/UserService_20260727_123456.class

# 输出示例：
# retransform success, size: 1, classes:
# com.example.UserService

# 4. 验证修复结果
# 使用 watch 命令观察方法执行
watch com.example.UserService someMethod "{params,returnObj}" -x 2

# 或使用 trace 命令追踪调用链
trace com.example.UserService someMethod
```

### 生成的脚本详解

**脚本结构：**
```bash
#!/bin/bash
# === 头部注释 ===
# 类名、生成时间、SHA-256 值

# === Base64 数据块 ===
CLASS_DATA="H4sIAAAAAAAA/+2d...（数千行）...AAAAA=="

# === 解码解压 ===
TEMP_CLASS="/tmp/UserService_20260727_123456.class"
echo "$CLASS_DATA" | base64 -d | gunzip > "$TEMP_CLASS"

# === SHA-256 校验 ===
EXPECTED_SHA="a1b2c3d4..."
if command -v sha256sum >/dev/null 2>&1; then
    echo "$EXPECTED_SHA $TEMP_CLASS" | sha256sum -c
elif command -v shasum >/dev/null 2>&1; then
    echo "$EXPECTED_SHA $TEMP_CLASS" | shasum -a 256 -c
else
    echo "Warning: No SHA-256 tool found, skipping verification"
fi

# === 打印使用提示 ===
echo "File ready at: $TEMP_CLASS"
echo "In Arthas, execute:"
echo "  retransform $TEMP_CLASS"
```

**为什么使用 gzip + Base64？**
- gzip：减小文件体积（原始 .class 文件通常 5-50KB，压缩后节省 50-70%）
- Base64：确保脚本是纯文本，可以通过任何文本传输工具（邮件、Slack、微信）

**为什么需要 SHA-256 校验？**
- 确保文件在传输过程中未损坏
- 避免复制粘贴时丢失部分内容
- 校验失败时脚本会报错，不会执行错误的热替换

### 注意事项

#### Arthas 的限制
- ✅ **支持**：修改方法体（方法实现逻辑）
- ❌ **不支持**：
  - 新增/删除字段
  - 新增/删除方法
  - 修改类继承关系
  - 修改方法签名（参数、返回值类型）

#### 使用建议
- ⚠️ 热修复是临时方案，应尽快发布正式版本
- ⚠️ 修复后建议记录变更，避免下次发布时遗漏
- 💡 可以使用 `jad` 命令反编译验证热替换是否生效：
  ```bash
  # 在 Arthas 中
  jad com.example.UserService someMethod
  ```
- 💡 建议在测试环境先验证热修复脚本，再应用到生产

#### 故障排查
**问题 1：找不到 .class 文件**
```bash
# 解决方案：确保项目已编译
# Maven
mvn clean compile

# Gradle
./gradlew clean build

# IntelliJ
Build → Rebuild Project
```

**问题 2：脚本执行失败（SHA-256 校验失败）**
- 原因：脚本内容在复制粘贴时损坏
- 解决方案：使用 SCP/SFTP 上传，不要复制粘贴脚本内容

**问题 3：Arthas retransform 失败**
- 检查是否修改了类结构（字段、方法签名）
- 检查 .class 文件是否与运行中的类版本匹配
- 查看 Arthas 详细错误信息

---

## ⚙️ GitLab/GitHub 集成配置

### GitLab 配置

**首次使用时会提示输入 Personal Access Token：**

1. 访问你的 GitLab 实例：`用户设置` → `Access Tokens`
2. 创建新 Token：
   - **Token name**：IDEA Plugin
   - **Scopes**：勾选 `api`
3. 点击 **"Create personal access token"**
4. 复制生成的 Token
5. 粘贴到插件提示框中

### GitHub 配置

**首次使用时会提示输入 Personal Access Token：**

1. 访问 GitHub：`Settings` → `Developer settings` → `Personal access tokens` → `Tokens (classic)`
2. 点击 **"Generate new token (classic)"**
3. 配置：
   - **Note**：IDEA Plugin
   - **Scopes**：勾选 `repo`（完整仓库访问权限）
4. 点击 **"Generate token"**
5. 复制生成的 Token
6. 粘贴到插件提示框中

### Token 存储位置

Token 会安全存储在系统密钥链中：
- **Windows**：Windows 凭据管理器
- **macOS**：钥匙串访问（Keychain）
- **Linux**：系统密钥环或加密文件

---

## ❓ 常见问题

### Q1：变基失败出现冲突怎么办？

**A**：插件会显示错误信息，需要手动解决冲突：

```bash
# 1. 查看冲突文件
git status

# 2. 手动编辑冲突文件，解决冲突标记
# <<<<<<< HEAD
# 你的代码
# =======
# 目标分支的代码
# >>>>>>> origin/master

# 3. 标记为已解决
git add <冲突文件>

# 4. 继续变基
git rebase --continue

# 5. 推送
git push --force-with-lease
```

### Q2：为什么 AI 生成提交消息失败？

**A**：检查以下项：

- ✅ API Key 是否正确
- ✅ Base URL 是否可访问（网络连接）
- ✅ Model ID 是否存在
- ✅ API 配额是否充足

使用设置页面的 **"Test Connection"** 按钮进行诊断。

### Q3：找不到 .class 文件怎么办？

**A**：确保项目已编译：

- Maven 项目：运行 `mvn compile`
- Gradle 项目：运行 `./gradlew build`
- IDEA：`Build` → `Build Project`

编译后的 `.class` 文件位置：
- Maven：`target/classes/`
- Gradle：`build/classes/kotlin/main/` 或 `build/classes/java/main/`

### Q4：支持源文件直接生成热修复脚本吗？

**A**：支持！插件会自动查找对应的 `.class` 文件：

1. 在 `.java` 或 `.kt` 源文件上右键
2. 选择 **"Generate Arthas Hotfix Script"**
3. 插件会在常见输出目录中搜索对应的 `.class` 文件
4. 如果找到多个（如内部类），会列出供选择

### Q5：创建 MR/PR 时提示 401 Unauthorized？

**A**：Token 可能过期或权限不足：

1. 检查 Token 是否过期
2. 确认 Token 权限：
   - GitLab：需要 `api` scope
   - GitHub：需要 `repo` scope
3. 重新生成 Token 并更新

删除旧 Token：
- Windows：`控制面板` → `凭据管理器` → 删除相关凭据
- macOS：`钥匙串访问` → 搜索 `GitLabToken` 或 `GitHubToken` → 删除

---

## 📚 更多信息

- **完整文档**：[README_CN.md](README_CN.md)
- **项目结构**：[CLAUDE.md](CLAUDE.md)
- **需求文档**：[docs/REQUIREMENTS.md](docs/REQUIREMENTS.md)

---

## 💡 快速提示

### 推荐工作流

**日常开发：**
```
修改代码 → Ctrl+K → AI生成提交消息 → 变基并推送
```

**提交 PR：**
```
修改代码 → Ctrl+K → 勾选"自动创建 MR" → 变基并推送
```

**生产热修复：**
```
修复代码 → 编译 → 右键 .class → 生成脚本 → 上传服务器 → Arthas retransform
```

### 快捷键

- **打开提交窗口**：`Ctrl+K`（Windows/Linux）、`Cmd+K`（Mac）
- **打开设置**：`Ctrl+Alt+S`（Windows/Linux）、`Cmd+,`（Mac）

### 性能优化建议

- 对于大型项目，建议在设置中启用 **"Auto Stash"**，避免未提交文件影响变基
- AI 生成提交消息时，使用 `gpt-3.5-turbo` 速度更快，`gpt-4` 质量更高
- 本地模型（Ollama）响应最快，但质量取决于模型

---

**最后更新**：2026-07-27  
**插件版本**：1.0.5  
**支持平台**：IntelliJ IDEA 2025.3.5+