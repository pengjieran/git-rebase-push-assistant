# Git Rebase & Push Assistant 使用文档

## 插件简介

Git Rebase & Push Assistant 是一个功能强大的 IntelliJ IDEA 插件，提供两大核心功能：

1. **Git 变基与推送助手** - 简化 Git 工作流，自动完成变基、提交、推送和创建 Merge Request/Pull Request
2. **Arthas 热修复生成器** - 为 Java/Kotlin 应用生成阿里 Arthas 热修复脚本

---

## 功能一：Git 变基与推送

### 功能概述

在 Git 提交对话框中新增「变基并提交推送」操作，自动执行以下流程：
- 拉取远程最新代码（fetch）
- 变基到目标分支（rebase，可选）
- 提交本地更改（commit）
- 安全强制推送（`--force-with-lease`）
- 可选自动创建 GitLab MR 或 GitHub PR

### 使用步骤

#### 1. 基本使用

1. 在 IntelliJ IDEA 中进行代码修改
2. 打开 Git 提交对话框（快捷键：`Cmd+K` / `Ctrl+K`）
3. 在提交按钮旁找到「变基并提交推送」按钮
4. 点击按钮，弹出配置对话框

#### 2. 配置对话框说明

![配置对话框示意]

**基本配置：**
- **目标分支**：选择要变基到的远程分支（如 `origin/main`、`origin/develop`）
- **提交消息**：输入提交说明
  - 可点击「AI 生成提交消息」按钮，基于代码差异自动生成规范化的中文提交消息
- **变基到目标分支**：开关按钮
  - ✅ 开启：执行 fetch → rebase → commit → `push --force-with-lease`
  - ❌ 关闭：跳过 fetch 和 rebase，执行 commit → 普通 `push`

**高级选项：**
- **Autostash**：变基前自动暂存未提交的修改
- **创建 Merge Request / Pull Request**：推送成功后自动创建 MR/PR
  - MR 标题：默认使用提交消息
  - MR 描述：可添加详细说明

#### 3. 执行流程

点击「开始」后，对话框实时显示执行进度：

```
正在添加文件...
正在提交更改...
正在拉取远程分支...
正在变基...
正在推送到远程仓库...
正在创建 Merge Request...
完成！
```

- ✅ 成功：2 秒后自动关闭对话框，显示通知（可在设置中配置）
- ❌ 失败：显示错误信息，对话框保持打开，可点击「关闭」按钮

### 平台支持

插件自动检测远程仓库平台：

- **GitLab**：远程 URL 不含 `github.com`
  - 支持自托管 GitLab 实例
  - 支持子组路径（如 `group/subgroup/project`）
  - API 端点：`{gitlab_url}/api/v4/projects/{project_path}/merge_requests`

- **GitHub**：远程 URL 包含 `github.com`
  - API 端点：`https://api.github.com/repos/{owner}/{repo}/pulls`
  - 自动检查重复 PR，避免重复创建

**Token 配置：**

首次创建 MR/PR 时，插件会提示输入访问令牌：

- **GitLab Token**：在 GitLab 中生成 Personal Access Token（权限：`api`）
- **GitHub Token**：在 GitHub 中生成 Personal Access Token（权限：`repo`）

Token 安全存储在系统钥匙串中（macOS Keychain / Windows Credential Manager），无需重复输入。

### 常见场景

#### 场景 1：基于 main 分支开发新功能

```
1. 切换到功能分支：git checkout -b feature/new-feature
2. 编写代码并测试
3. 提交时使用「变基并提交推送」
   - 目标分支：origin/main
   - 启用「变基到目标分支」
   - 启用「创建 Merge Request」
```

#### 场景 2：快速修复不需要变基

```
1. 在当前分支快速修改代码
2. 提交时使用「变基并提交推送」
   - 关闭「变基到目标分支」开关
   - 直接推送到当前分支
```

#### 场景 3：修改已推送的提交

```
1. 修改代码后需要覆盖远程提交
2. 使用「变基并提交推送」
   - 目标分支：origin/main
   - 启用「变基到目标分支」
   - 使用 --force-with-lease 安全强制推送
```

---

## 功能二：Arthas 热修复生成器

### 功能概述

为 Java/Kotlin 应用生成阿里巴巴 Arthas 工具的热修复脚本，无需重启服务即可在生产环境修复 Bug。

### 使用步骤

#### 1. 选择文件

在以下任意位置右键点击 `.java`、`.kt` 或 `.class` 文件：
- **项目视图**（Project 工具窗口）
- **编辑器**
- **编辑器标签页**
- **工具菜单**（Tools → Generate Arthas Hotfix Script）

#### 2. 源文件自动定位

插件支持从源文件自动定位编译后的 `.class` 文件：

**支持的构建工具：**
- **Maven**：`target/classes/`
- **Gradle（Kotlin）**：`build/classes/kotlin/main/`
- **IntelliJ 编译输出**：`out/production/模块名/`

**示例：**
```
源文件：src/main/java/com/example/UserService.java
自动查找：
  → target/classes/com/example/UserService.class
  → build/classes/kotlin/main/com/example/UserService.class
  → out/production/main/com/example/UserService.class
```

找不到 `.class` 文件时，插件会显示错误提示。

#### 3. 生成脚本

选择文件后，插件自动生成 Base64 编码的 Shell 脚本，包含：
- Gzip 压缩的 `.class` 文件内容
- SHA-256 校验和（验证文件完整性）
- Arthas `retransform` 命令

#### 4. 脚本输出对话框

生成完成后弹出对话框，提供以下操作：

- **复制到剪贴板**：一键复制脚本内容
- **保存到文件**：将脚本保存为 `.sh` 文件
- **关闭**：关闭对话框

### 使用生成的脚本

#### 步骤 1：连接到生产环境

```bash
# SSH 连接到生产服务器
ssh user@production-server
```

#### 步骤 2：启动 Arthas

```bash
# 下载并启动 Arthas（首次使用）
curl -O https://arthas.aliyun.com/arthas-boot.jar
java -jar arthas-boot.jar

# 选择要诊断的 Java 进程
```

#### 步骤 3：执行热修复脚本

```bash
# 将生成的脚本内容粘贴到终端并执行
bash << 'EOF'
# ... 插件生成的脚本内容 ...
EOF
```

脚本执行流程：
1. 解码 Base64 内容
2. 解压 Gzip 数据到 `/tmp/UserService.class`
3. 校验文件 SHA-256 值
4. 打印 Arthas 命令

#### 步骤 4：应用热修复

在 Arthas 控制台执行打印的命令：

```bash
retransform /tmp/UserService.class
```

#### 步骤 5：验证修复

```bash
# 查看重定义结果
retransform -l

# 测试修复后的行为
# ...
```

### 安全说明

- ⚠️ **生产环境慎用**：热修复会立即修改运行中的类，确保充分测试
- ✅ **校验机制**：脚本包含 SHA-256 校验，防止文件传输损坏
- ✅ **可回滚**：Arthas 支持撤销热修复（`retransform --deleteAll`）
- ⚠️ **权限要求**：需要对 Java 进程有足够的操作权限

### 常见场景

#### 场景 1：紧急修复生产 Bug

```
1. 在本地修改代码并编译
2. 右键编辑器中的 .java 文件
3. 选择「Generate Arthas Hotfix Script」
4. 复制生成的脚本
5. SSH 到生产服务器并执行
6. 验证修复效果
7. 立即发版更新代码
```

#### 场景 2：临时验证修复方案

```
1. 编写测试修复代码
2. 生成 Arthas 脚本并应用到测试环境
3. 验证修复效果
4. 确认后再正式发版
```

---

## 插件设置

### 打开设置面板

**路径**：`IntelliJ IDEA → Settings / Preferences → Tools → Git Rebase & Push`

### 配置项说明

#### Git 变基设置

- **默认目标分支**：提交对话框中的默认分支（如 `origin/main`）
- **默认启用 Autostash**：变基前自动暂存未提交更改
- **成功时显示通知**：推送成功后弹出通知气泡

#### AI 提交消息生成

- **OpenAI API 端点**：兼容 OpenAI API 的服务地址
  - 默认：`https://api.openai.com/v1/chat/completions`
  - 可配置为其他兼容服务（如 Azure OpenAI、本地部署等）
- **OpenAI 模型**：使用的模型名称（如 `gpt-4o`、`gpt-3.5-turbo`）
- **OpenAI API Key**：访问令牌（安全存储在系统钥匙串中）

#### GitLab / GitHub 配置

- **访问令牌**：在首次创建 MR/PR 时配置，存储在系统钥匙串中
- **查看/更新令牌**：可在设置面板中重新输入

---

## 故障排查

### Git 变基失败

**问题**：变基过程中出现冲突

**解决方案**：
1. 对话框显示错误信息
2. 手动解决冲突：
   ```bash
   git status
   # 编辑冲突文件
   git add .
   git rebase --continue
   git push --force-with-lease
   ```

### 创建 MR/PR 失败

**问题**：Token 权限不足或已过期

**解决方案**：
1. 检查 Token 权限：
   - GitLab：需要 `api` 权限
   - GitHub：需要 `repo` 权限
2. 重新生成 Token 并在设置中更新

### Arthas 脚本执行失败

**问题**：找不到 `.class` 文件

**解决方案**：
1. 确保项目已编译：`./gradlew build` 或 `mvn compile`
2. 检查输出目录是否正确
3. 手动选择 `.class` 文件而非源文件

**问题**：SHA-256 校验失败

**解决方案**：
1. 确认复制脚本时未截断内容
2. 重新生成脚本并完整复制

---

## 常见问题 (FAQ)

### Q1：插件支持哪些 IntelliJ 平台产品？

**A**：支持所有基于 IntelliJ 平台的 IDE，包括：
- IntelliJ IDEA（Ultimate / Community）
- PyCharm
- WebStorm
- Android Studio
- 等等

### Q2：是否支持多仓库项目？

**A**：当前版本仅操作第一个 Git 仓库（`repositories.firstOrNull()`），多仓库项目需手动切换。

### Q3：`--force-with-lease` 与 `--force` 的区别？

**A**：
- `--force`：强制覆盖远程分支，可能丢失他人的提交
- `--force-with-lease`：仅当远程分支与本地预期一致时才推送，更安全

### Q4：AI 生成的提交消息准确吗？

**A**：基于 Git diff 内容生成，适用于常见场景。复杂变更建议人工审核和调整。

### Q5：Arthas 热修复有哪些限制？

**A**：
- 不支持修改类结构（添加/删除字段或方法）
- 不支持修改方法签名
- 仅支持修改方法体内的逻辑

### Q6：生成的脚本可以重复使用吗？

**A**：可以。脚本是自包含的，只要 `.class` 文件未变化，可以多次执行。

---

## 系统要求

- **IntelliJ 平台版本**：2025.3.5 或更高
- **JDK 版本**：17 或更高
- **Git 版本**：2.0 或更高
- **Arthas 版本**：3.0 或更高（热修复功能）

---

## 许可证与支持

本插件为开源项目，遵循项目仓库中的许可证声明。

**问题反馈与功能建议**：
- GitHub Issues: [项目仓库地址]
- 贡献代码：欢迎提交 Pull Request

---

## 更新日志

参见项目根目录的 `CHANGELOG.md` 文件。

---

**祝您使用愉快！**
